[pytest]
# Автоматический режим pytest-asyncio: все async def тесты выполняются
# без необходимости помечать каждый @pytest.mark.asyncio
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
